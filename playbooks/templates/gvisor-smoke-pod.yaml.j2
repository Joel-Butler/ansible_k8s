apiVersion: v1
kind: Pod
metadata:
  name: gvisor-smoke
  namespace: default
spec:
  runtimeClassName: gvisor
  restartPolicy: Never
  containers:
    - name: smoke
      image: docker.io/library/busybox:1.36
      command: ["/bin/sh", "-c", "sleep 300"]
