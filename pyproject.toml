[project]
name = "ansible-k8s"
version = "0.0.0"
description = "Ansible playbooks for managing a homelab Kubernetes cluster."
requires-python = ">=3.13"
dependencies = [
    "ansible",
    "ansible-lint",
]

[tool.uv]
package = false
